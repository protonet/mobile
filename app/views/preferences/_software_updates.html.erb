update your nodes software<br>
you are running on release  : <bold><%= SystemRelease.current_version %>/<%= SystemRelease.current_type %></bold><br>
latest stable release is    : <bold><%= SystemRelease.latest_version %></bold><br>
latest beta   release is    : <bold><%= SystemRelease.latest_beta_version %></bold><br>
software update is available: <bold><%= !SystemRelease.most_recent? %></bold><br>
<br />
<%= form_tag(system_release_update_path) do %>
  <%= label_tag          :password,          'Enter your *NODE* password:' %><br />
  <%= password_field_tag :password, nil %><br />
  <%= select_tag :release, options_for_select(SystemRelease.last_releases.reverse.map{|c| ["#{c[0]} / #{c[1]}", c[0]] if c[0].to_i >= SystemRelease.current_version}.compact, SystemRelease.last_releases.reverse.find {|release| release[1].strip! == "STABLE"}[0]) %>
  and hit <%= button("update") %>
<% end %> 
