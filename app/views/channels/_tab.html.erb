<li>
  <%= link_to channel.display_name || channel.name.capitalize, { :channel_id => channel.id }, {
    :title => channel.description,
    :class => channel.locally_hosted? ? (channel.global? ? 'global-hosted' : 'local') : 'global',
    'data-channel-id' => channel.id
  } %>
</li>