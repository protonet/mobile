<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <title><%= t("captive.title_check") %></title>
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <style>
    html {
      background: #aaa;
      height: 100%;
    }

    @font-face {
      font-family: protonet;
      font-style: normal;
      font-weight: normal;
      src: url('/css/fonts/univers_light_condensed.eot'); /* IE 9 compat */
      src: url('/css/fonts/univers_light_condensed.eot?#iefix') format('embedded-opentype'), /* IE6-IE8 */
           url('/css/fonts/univers_light_condensed.woff') format('woff'), /* Modern Browsers */
           url('/css/fonts/univers_light_condensed.ttf') format('truetype'), /* Safari, Android, iOS */
           url('/css/fonts/univers_light_condensed.svg') format('svg'); /* Legacy iOS */
    }

    body {
      min-height: 100%;
      border-top: 1px solid transparent;
      margin-top: -1px;
      position: relative;
      overflow-y: scroll;
      background: #e8e8e8;
      color: #000;
      text-align: center;
      font-family: Verdana, sans-serif;
    }

    div.dialog {
      width: 25em;
      padding: 1em 4em;
      margin: 4em auto 0 auto;
      box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
    }

    h1 { 
      text-shadow: 0 1px 1px white;
      font-weight: normal;
      font-size: 38px;
      line-height: 1.3;
      font-family: protonet, Verdana, sans-serif;
    }
  </style>
  <script>
    if(window.parent == window) {
      document.location="http://<%= address_for_current_interface + (Rails.env.production? ? "" : ":#{configatron.web_app_port}") %>/?captive_redirect_url=" + encodeURIComponent(document.location.href);
    }
  </script>
</head>

<body>
  <div class="dialog">
    <img src="/img/protonet_node_front_view.png" height="249" width="360" alt="<%= t("instruments.hint_protonet_node") %>">
    <%= raw(t("captive.text_check",
      :name => Node.local.name,
      :href => "http://" + address_for_current_interface + (Rails.env.production? ? "" : ":#{configatron.web_app_port}") + "/"
    )) %>
  </div>
</body>
</html>
