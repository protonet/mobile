<li>
  <%- if invitation.invitee %>
  <%= avatar(invitation.invitee, :width => 20, :height => 20) %>
  <strong>(<%= invitation.invitee.login %>)</strong>
  <%- end %>
  <%= invitation.email %>
  
  <%= link_to "delete", invitation_path(invitation), :class => "action", :method => :delete, :confirm => "Are you sure?" %>
  
  <%= link_to "show", invitation_path(invitation), :class => "action" %>
</li>