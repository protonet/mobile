<%= stylesheet_tag("/css/pages/#{controller_name}", :cache => (configatron.avoid_caching != false)) if File.exists?(File.join(Rails.root, 'public/css/pages', "#{controller_name}.css")) %>

<%= yield %>

<%= sprockets_include_tag(controller_name.to_sym) rescue nil %>