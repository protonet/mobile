require File.join(File.dirname(__FILE__), '..', "test_helper")

class BackendTest < Test::Unit::TestCase

  context 'Backend' do

    test "should define a series of api calls" do
    end

    test "should have a backend connection settable as a class method" do
    end

    test "should forward all defined api methods to the connected backend adapter with complete args" do
    end

    test "should raise a standard method not defined error when a method is called that is not part of the api" do
    end

  end

end
