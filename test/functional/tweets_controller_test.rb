require 'test_helper'

class TweetsControllerTest < ActionController::TestCase

  test "that a tweet gets created with a guest user name if no user is logged in" do
    flunk
  end
  
  test "that a tweet has an user nil when a logged out user created it" do
    flunk
  end

  test "that a tweet gets created with the users login as an author if he is logged in" do
    flunk
  end
  
  test "the tweet should be assigned to the default channel (home)" do
    flunk
  end
  
end
